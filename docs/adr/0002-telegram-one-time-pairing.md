# Telegram Account Pairing via One-Time Code

We decided to pair Telegram user accounts with Up2D8 accounts using a 6-digit one-time code (`/start 123456`) generated inside the mobile app. This avoids requiring students to log in to Telegram via OAuth inside the app, eliminating credential handling risks while ensuring secure 1-to-1 account mapping.
